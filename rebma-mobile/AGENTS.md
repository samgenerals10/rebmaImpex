# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Upgraded from SDK 56 to SDK 57 (React Native 0.86.3) via `npx expo install expo@^57.0.0 --fix`, per Expo's own SDK 57 changelog stating no breaking changes from RN 0.85. `npx expo-doctor@latest` passed 21/21 after the upgrade — no manual package pinning needed beyond what `--fix` did automatically.
